const initialState = {
    tasks: [],
    loading: false,
  };
  
  export default function taskReducer(state = initialState, action) {
    switch (action.type) {
      default:
        return state;
    }
  }
  